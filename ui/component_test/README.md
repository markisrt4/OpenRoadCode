# UI Focus Component Test

Run from the repository root:

```bash
python -m ui.component_test.focus_manager_cli
```

The test verifies:

- Initial focus selection
- Forward and backward traversal
- Skipping disabled items
- Wrap-around behavior
- Activation of the focused item
